# By default, Valkey uses the following default for binding to interfaces on the server: bind 127.0.0.1 -::1
#bind 127.0.0.1 -::1

# development conf --- comment out on production
bind 0.0.0.0 -::1
protected-mode no
